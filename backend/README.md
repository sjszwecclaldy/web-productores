# Backend — API del portal

Ver [README principal](../README.md) para configuración y deploy.

```bash
npm install
cp .env.example .env
npm run dev
```

El esquema SQL se aplica automáticamente al iniciar (`sql/schema.sql`).
